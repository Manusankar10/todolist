class Todo {
  String title;
  String description;
  bool isCompleted;
  DateTime date;

  Todo({
    required this.title,
    required this.description,
    required this.isCompleted,
    required this.date,
  });
}
